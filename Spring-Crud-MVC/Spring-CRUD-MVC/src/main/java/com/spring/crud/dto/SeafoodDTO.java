package com.spring.crud.dto;

/**
 * Необходимо реализовать DTO-класс МОРЕПРОДУКТ.
 * <p>
 * Требования по полям: id(идентификатор), productName(название продукта), weight(вес), deleted(актуальность в БД).
 * Типы: id - Long, productName - String, weight - Double, deleted - boolean.
 * Реализовать инкапсуляцию для класса и его полей.
 * <p>
 * <p>
 * Валидация полей:
 * productName - ограничение на пустоту строки, а так же на длину строки(мин - 6, макс - 20),
 * weight - ограничение на минимальное значение - 100,
 */
public class SeafoodDTO {
}